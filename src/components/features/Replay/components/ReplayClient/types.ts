/**
 * Information about a command.
 */
export interface CommandInfo {
    /** The round number of the command. */
    roundNumber: number;
    /** The description of the command. */
    description: string;
  }